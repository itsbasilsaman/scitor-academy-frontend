import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["www.filmibeat.com", "www.skillstork.org", "images.unsplash.com", "www.westend61.de"],
  },
};

export default nextConfig;
